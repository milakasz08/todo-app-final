{{ form_start(form, {'attr': {'class': 'space-y-5 max-w-md'}}) }}
    {{ form_widget(form) }}
    <button class="bg-indigo-600 hover:bg-indigo-700 text-white px-4 py-2 rounded-xl text-sm font-semibold shadow-sm transition">{{ button_label|default('common.save'|trans) }}</button>
{{ form_end(form) }}
